# Ultrawork Notepad — v07-durable-execution-substrate (issue #118)
Started: 2026-09-18T00:00:00+09:00

## Plan (exhaustive, atomic)
1. Claim issue #118 (intent-cli worker claim)
2. Wiring-confirmation relay → w1X:p1
3. Fetch issue #118 body; explore code-map paths
4. Plan agent → parallel task graph
5. TDD impl: (1) Durable Task/Run continuation, (2) Budget & circuit breaker, (3) Evidence-first review gate
6. Reviewer Gate (ultrabrain/plan reviewer)
7. PR ready-for-review
8. intent-cli worker result-summary + complete
9. Completion relay → w1X:p1

## Scenarios (the contract)
- S1 (resume-after-crash): task_id で中断 goal を resume、成果物・失敗理由・再開地点が SQLite 永続化 → binary: resume 後の run が中断地点から継続し events replay が task state を再構築
- S2 (budget checkpoint): 50-call ごとに checkpoint event 発火 → binary: mock provider e2e で DiagnosticEvent 発行を観測
- S3 (budget exhausted): tool-call 予算超過 → DiagnosticEvent code=BudgetExhausted; 無進捗 → NoProgress
- S4 (provider pre-spawn gate): 未検証 fallback で worker 起動しない → ProviderUnavailable 発行
- S5 (evidence-first verdict): ReviewLoop verdict が typed tool result (criterion+evidence 紐付け)
- S6 (regression): cargo test --workspace 全 green

## Now
Claim issue #118

## Todo (remaining, ordered)
(see Plan)

## Findings
- W2-T5 GREEN (2026-09-18): goal_supervisor_continuation 11/11 (指定3件含む)、goal_persistence 3/3、goal_recovery 1/1、goal_state_machine 13/13。storage 162 tests + 8 doctests pass。cargo check -p runtime、cargo clippy -p runtime -p storage -- -D warnings、cargo fmt -p runtime -p storage -- --check、変更 Rust 10 files の LSP error diagnostics、diff check pass。最初の persistence 実行は fixture の Started 不足で FK violation となり、fixture 修正後成功。Clippy collapsible_match 修正後成功。
- W2-T5 contract: resume_task/retry_task/cancel_task は既存 actor command。受付時 run/attempt と actor 現在値を照合し重複 dispatch を拒否。新 RunId と attempt generation を割当、task_id/親/role/purpose を保持し RunConfig.task_id を設定。再開コンテキストは typed TaskContinuation JSON として実モデル呼出しへ渡す。Cancelled/Completed は再試行不可。上限は既存 max_continuations を共有。TaskProgressed の型付き status/input/cursor/artifact/failure/attempts を SQLite reconcile が反映し、reopen テストで検証。旧世代 progress/checkpoint と重複 retry event は ledger を巻き戻さない。
- W2-T5 integration limits: task 作成側は TaskProgressed.progress に storage::entity::TaskContinuation を設定する必要あり（任意 JSON の既存 progress は引き続き replay 可、command は typed state のみ実行）。既存 storage bridge の append_event/reconcile が persistence 境界であり supervisor 自身は DB を開かない。T6 stale と budget diagnostics の failure/cursor 発行、GUI command 配線は後続担当。retry は保存 context の新規実行で、旧 tool snapshot や isolated worktree 所有権を復元しない。既存 run-specific config の完全復元は未対応。
- W2-T5 self-review: actor task module 225、test helper 208 pure LOC（次の成長前に分割）、projection durable 62、typed payload 44、continuation 59、ledger durable 195。既存 supervisor 1606 / test entry 338 / entity 726 / projection 260 は全面分割せず必要配線のみ。新規 unwrap()/expect()/allow/unsafe/narrowing cast は production に無し。型付き境界・世代照合・BDD tests・モデルに到達する cursor を検証。プロンプト prose の assertion は追加せず JSON payload を parse して比較。並行 T10 の staged changes はその担当による commit を待ち、混入させない。
- W2-T10 RED (2026-09-18): the three requested review_loop tests were added before production edits. After correcting the CriterionEvidence import to event_bus::orchestrator, `cargo test -p runtime --test review_loop` exited 101 with missing parse_reviewer_output, missing ReviewResult Deserialize, and missing on_review_result (E0432/E0277/E0599). An additional lesson test failed behaviorally with the old memory_queue parser (0 passed / 1 failed: entries were not Promoted); that was a temporary old-parser regression check after the review implementation, not an initial RED claim.
- W2-T10 GREEN: review_loop 8/8, post_run_learning 4/4, orchestrator_flow 2/2, orchestrator_loop_e2e 4/4 passed (18 total). cargo check -p runtime, targeted rustfmt --check for all five owned Rust files, their LSP error diagnostics, and git diff --check passed. Broader finish_gate_table could not compile because its existing CriterionCheck literal lacks evidence. Crate clippy -D warnings stopped in concurrent ledger/durable.rs:65 (collapsible_match); crate fmt stopped in concurrent ledger/supervisor/support files. Those files were not modified by T10. Earlier GREEN attempt also hit concurrent continuation.rs private TaskContinuation access, subsequently resolved by its owner.
- W2-T10 API for T11: deserialize the extracted structured JSON directly into public ReviewResult (`serde_json::from_value::<ReviewResult>`); `parse_reviewer_output(Option<ReviewResult>, &str)` prefers Some over contradictory prose, otherwise accepts raw JSON then the existing fenced JSON parser. Pass a valid result to `ReviewLoop::on_review_result(result, head_sha, reviewer_run_id)`; legacy on_reviewer_done remains and fail-closes on unparsable text. Supervisor extraction/integration is intentionally untouched. CriterionWire uses Option<event_bus::orchestrator::CriterionEvidence> with serde default, preserving all six evidence fields into GateEvidence::Criteria and preserving evidence when unmet/unknown converts approve into request-update. Max rounds and repeated-findings behavior share the same existing state machine.
- W2-T10 memory/prompt contract: reviewer prompts request actual command/exit_status/target_sha plus optional diff_ref/artifact_path/red_evidence; absent evidence stays compatible as None. LearningQueue uses the common raw-typed-first/fenced fallback parser, preserving nonempty/all-met/approve plus exact criterion.id == lesson.evidence promotion checks. No new tool, provider transport, or supervisor extraction was added. No prompt prose assertions. Owned pure LOC: review 206 (warning band; split wire parsing before further growth), prompts 111, memory_queue 145, review_loop tests 176, post_run_learning tests 155. No new unsafe, production unwrap, narrowing casts, lint suppressions or logging; evidence uses the existing event-bus type rather than duplicating the boundary schema.
- W3-T11 RED/GREEN (2026-09-18): renamed the existing typed-vs-prose review-loop regression to `supervisor_prefers_typed_tool_result_over_prose`; the pre-supervisor targeted run passed because T10's parser API already proves the precedence seam. T10's original missing-API RED remains recorded above. Supervisor completion now parses `run_result` as `serde_json::Value` then `ReviewResult`, passes typed results through `parse_reviewer_output` and `on_review_result`, and retains `on_reviewer_done` for prose/unparsable fallback. Spawn prompt already required criterion evidence fields, so it was left unchanged. Max-round behavior and non-review modules were untouched.
- W2-T5 RED (2026-09-18): production edits 前に指定3テストを追加し `cargo test -p runtime --test goal_supervisor_continuation` exit 101。E0599 x6: SupervisorHandle の resume_task / retry_task / cancel_task 未実装。既存ライブラリのコンパイルは成功し、意図した missing-API RED を確認。
- W1-T9 RED: initial `cargo test -p runtime --test provider_verify` stopped at T4's five E0004/E0063 library errors, not the missing verify API. Providers RED independently confirmed E0425 `verify_connectivity` missing. After T4 landed, Codex regression produced `{}` vs `{"codex"}` before OAuth-aware verification. Streaming rejection is additional regression coverage, not independently RED-first.
- W1-T9 GREEN: providers 302/302 (including list_models 4, codex_models 6, connectivity 2); provider_verify 6/6; orchestrator_loop_e2e 4/4; compose unit tests 25/25. `cargo clippy -p runtime -p providers -- -D warnings`, `cargo fmt -p runtime -p providers -- --check`, changed-file LSP error diagnostics and diff check passed. Existing compose stub fixtures now inject verified state rather than hitting their deliberately dead endpoint; the 16 failures noted by T7 are resolved.
- W1-T9 contract: `RoutedModel::verify_candidates` verifies every composed profile (superset of role and cross-role fallback candidates, also explicit preferences). OnceCell serializes concurrent initializers and retains success/failure for the composition lifetime; identical normalized endpoint/auth/account combinations share a ping. Requests resolve first, then reject failed candidates before send/send_streaming with one ProviderUnavailable diagnostic and RuntimeError::Model. Explicit preferences remain authoritative; no automatic retry/fallback policy was added. Composition bus covers non-streaming errors. Codex uses stored OAuth token/account and existing list_codex_models headers; HTTP error bodies are omitted from verification diagnostics to avoid token reflection.
- W1-T9 limits: connectivity proves authenticated catalog reachability, not availability of a particular inference model; model selection still follows existing router/catalog semantics. Codex verification reads stored tokens (no proactive refresh); failures stay cached until recomposition. Direct RoutedModel::new lacks a credential-store/bus handle, so Codex fails closed there and diagnostics require complete_streaming's bus. Gate is at the requested pre-send boundary, not before runtime Run allocation. T7's 400-tool-call setting is an observation threshold (strict >), not an existing hard cancellation cap; catalog GETs neither count as tools nor bypass budget handling. No budget edits.
- W1-T9 ownership/size: no ledger/supervisor/review or T7 edits. T4 compilation fixes arrived during verification; no same-file code conflict. New verification module 91 pure LOC, provider tests 24, provider_verify 236 and compose fixture 238 (warning band: split before growth); compose.rs retains its existing SIZE_OK exception. No new production unwrap/unsafe/casts, provider trait changes or logging framework. Shared notepad entries from other workers are preserved.
- W1-T4 RED (2026-09-18): added `replay_propagates_ledger_errors_instead_of_swallowing` and `replay_after_task_lifecycle_events_reconstructs_durable_state` before production edits. `cargo test -p runtime --test goal_state_machine` exited 101: E0063 missing CriterionCheck.evidence at review.rs:200 and E0004 at ledger.rs:342/705, supervisor.rs:1624/1660. These five library errors masked the integration test's missing checked API/fields; no separate missing-API diagnostic was produced.
- W1-T4 GREEN: requested tests pass 17/17 (goal_state_machine 13, goal_recovery 1, goal_persistence 3). `cargo check -p runtime`, `cargo clippy -p runtime -- -D warnings`, targeted rustfmt --check on all nine owned Rust files, LSP error diagnostics on all nine, and `git diff --check` pass. `cargo fmt -p runtime --check` was run twice: first blocked by concurrent budget_tracker_tests/budget_checkpoint formatting, latest by concurrent compose/verification.rs and provider_verify.rs formatting. Those files belong to other workers and were not staged.
- W1-T4 contract finding: T2 task variants intentionally have no goal_id; TaskRetryScheduled also has no prior run_id. Replay/live supervision resolve ownership through attached_runs plus durable task_runs; a retry requires earlier task progress/checkpoint/stale linkage or an already attached new_run_id. Unresolved/ambiguous ownership is a checked replay error, not broadcast to every goal. No event-bus/storage contract patch made. Task attempts preserve the event's explicit attempt number; retry run linkage inherits the previous attached run's parent/role/purpose.
- W1-T4 replay: checked API collects errors in event order, rejects duplicate creation instead of resetting snapshots, rejects orphan events, and isolates multiple goals. Runtime callers migrated; legacy replay remains an explicitly documented expect wrapper as allowed by the task (GUI callers outside scope remain source compatible). Recovery integration verifies every durable field plus task_runs survives actual supervisor recovery.
- W1-T4 size/self-review: durable.rs 167 pure LOC, replay.rs 53, review.rs 183, recovery test 139, replay_errors test 50, persistence test 211 (warning band). Existing ledger.rs 583, supervisor.rs 1596 and required goal_state_machine.rs 330 exceed the preferred ceiling; full decomposition deferred to avoid cross-wave overlap, with new logic extracted into bounded modules. No new unwrap/as narrowing/allow attributes, no wildcard discard of task semantics. The legacy replay expect is the explicit task-authorized exception. Extra edge tests are regression coverage added after initial GREEN, not claimed as separate RED-first proof.
- W1-T7 (2026-09-18): BudgetCounters lives in runtime/budget_tracker.rs, separate from rules injection bytes. RunConfig.budget defaults: 400 calls, 2 hours, 2,000,000 input+output tokens, 20 rereads (first read excluded), 20 consecutive tool rounds without a successful edit. Threshold crossing is strictly `>`; one diagnostic per threshold per run. Observation only: no automatic cancellation in T7. Existing detector proposes at 200, not a 400 hard cap; its sole counter now keeps counting after proposal latch, and supplies checkpoints at 50-call boundaries. Executed/invalid non-meta calls retain existing counting semantics; denied calls and meta operations are excluded.
- W1-T7 RED: pre-implementation `cargo test -p runtime --test budget_checkpoint` failed at the known T4 E0004 x4/E0063 x1, so behavioral RED was blocked. After T4 updates, mutation (disconnect count source to zero) produced 3 failures: checkpoint `[]` vs `[(50,150,100),(100,300,200)]`, budget diagnostic `0` vs `1`, and missing checkpoints through 250. Restored immediately. This is mutation evidence, not a claim that behavioral RED ran before implementation.
- W1-T7 GREEN: budget integration 7/7, budget unit 2/2, escalation 12/12, supervisor stall 6/6, orchestrator_loop_e2e 4/4. cargo check -p runtime, clippy -p runtime -- -D warnings, fmt -p runtime --check pass; all nine changed Rust files have clean LSP error diagnostics. Full cargo test -p runtime blocked by tests/interview.rs:109 TaskRecord missing eight T1 fields. A separate lib run had 350 passed/16 compose failures caused by concurrent T9 verification requesting http://127.0.0.1:1/v1/models. Those unrelated files were not edited.
- W1-T7 integration point: supervisor should set RunConfig.task_id to durable task ID when spawning/retrying, consume BudgetExhausted/NoProgress diagnostics by run_id, persist failure reason and checkpoint, cooperatively cancel active run, and mark task failed exactly once (ignore stale/terminal run diagnostics). TaskCheckpoint uses explicit task_id, then team_task.id, then standalone run_id fallback. Elapsed threshold is checked at loop/response/tool boundaries, not a watchdog during a hung provider/tool. No-progress uses successful edit rounds as progress signal; shell-only mutations are not inferred. Provider gate and supervisor reactions remain their owners' work.
- W1-T7 size/self-review: tracker 158 pure LOC, tests 198, helper 29, unit 25; detector 224 (warning band). Existing agent_loop/tool_calls retain their SIZE_OK exceptions. New code has no unsafe, unwrap(), narrowing casts, or lint suppression; typed BudgetSettings and shared count source avoid duplicate counters. Concurrent T4/T9 edits were observed and preserved; no owned-file conflict occurred.
- W2-T8 RED/GREEN (2026-09-18): added `escalation_latch_also_emits_single_NoProgress_diagnostic`; before the bridge it failed with 0 diagnostics while retaining one `EscalationProposed`, then passed after the latch hook emitted one `NO_PROGRESS` diagnostic with the triggering call ID. `ConsecutiveEditFailures` and `RepeatedFileRewrite` map to the bridge; `ToolCallThreshold` remains lifecycle-only. T7's budget tracker path was not duplicated or modified. Targeted escalation and supervisor-stall verification follows.
- T1 RED (2026-09-18): `cargo test -p storage --test migration v8_extends` failed with `left: 7 / right: 8` (0 passed, 1 failed). `cargo test -p storage --test task_queue` failed with E0560 for all eight missing durable fields (16 errors after correcting the test's close() return-type mistake). The repo update/list test also failed with E0560 before implementation. Projection RED failed with `Some(Failed)` vs `Some(Cancelled)` before changing the mapping.
- T1 GREEN: `cargo test -p storage` passed 162 unit/integration tests (87 unit + 75 integration), plus 8 doc-tests. Final migration-only rerun passed 8/8. `cargo check -p storage`, `cargo clippy -p storage -- -D warnings`, `cargo fmt --check`, and `git diff --check` passed. Storage LSP scan and targeted task/migration diagnostics were clean.
- T1 design: v3 already allowed pending/blocked; v8 makes all eight statuses bijective and preserves task_links during transactional table recreation. JSON is opaque Option<String> matching run_contexts; attempts is u32; heartbeat is Option<SystemTime>. Public entity::TaskRecord/TaskStatus paths remain stable via re-export. Repo tests cover update/list and NULL clearing; writer integration covers create/get/reopen. The v6 fixture now creates the complete earlier schema. Table/index names are unchanged.
- T1 size review: new task entity 36 pure LOC, task repo 181, migration tests 247, CRUD tests 246 (warning band; split before further additions). Existing oversized entity/projection files were reduced/not expanded; unrelated credential/projection refactoring remains out of scope.
- T2 (2026-09-18): event-bus に TaskProgressed / TaskCheckpoint / TaskRetryScheduled / TaskStaleMarked、CriterionEvidence、CriterionCheck.evidence (serde default)、event::diagnostic_codes の 3 定数を追加。DiagnosticEvent と既存 variant の形状は維持。既存 enum に ns timestamp はなく、elapsed は u64 / count は u32。指定どおり last_heartbeat_ns は u64。
- T2 RED: 実装前の `cargo test -p event-bus` は exit 101。`E0599: no variant named TaskProgressed/TaskCheckpoint/TaskRetryScheduled/TaskStaleMarked`、`E0422/E0425: CriterionEvidence not found`、`E0560: CriterionCheck has no field named evidence` の 11 エラー。診断テスト単独 `cargo test -p event-bus --test diagnostic` も exit 101、3 定数に対する `E0433: cannot find diagnostic_codes in event` を確認。
- T2 GREEN: `cargo test -p event-bus` は 148 passed / 0 failed (unit 120、integration 28、doc 0)。全 variant + Criteria evidence の Event envelope 往復、exit_status/target_sha/artifact_path の wire 値、旧 checklist の evidence 省略、optional evidence refs 省略、u64 最大値、負の exit_status、診断の直接 JSON/EventKind 往復を検証。
- T2 gates: `cargo check -p event-bus` / `cargo clippy -p event-bus -- -D warnings` / `cargo fmt -p event-bus --check` は exit 0。変更 Rust 3 ファイルの LSP error 診断なし。`cargo fmt --check` は並行 T1 の storage/src/repo/task.rs、storage/tests/migration.rs、storage/tests/task_queue.rs の整形差分のみで失敗し、担当外なので変更せず。後続は CriterionCheck の Rust literal に evidence を設定し、新 enum variant の exhaustive consumer を更新する必要あり。
- W1-GUI (2026-09-18): `cargo check -p gui` が `commands.rs:378` の `orchestrator_goal_id` match で、T2追加の `TaskProgressed` / `TaskCheckpoint` / `TaskRetryScheduled` / `TaskStaleMarked` に対する E0004（non-exhaustive patterns）で失敗。これらは goal_id を持たない task lifecycle event のため、既存の `ShellCommandDenied => None` に合わせて4 variantを明示的に `None` へ処理し、将来variantを隠す wildcard は使わない。
- W1-T3 verification-by-construction: `gate_evidence_criteria_checklist_round_trips_with_full_evidence` は `OrchestratorEvent::EvidenceRecorded` の Criteria checklist に command、exit_status、target_sha、diff_ref、artifact_path、red_evidence を全て設定し、実装変更なしで 1 passed。T2 の `GateEvidence::Criteria` → `Vec<CriterionCheck>` → `CriterionEvidence` serde 配線を direct event 往復の回帰テストとして固定した。
- W1-T3 legacy compatibility: `legacy_criterion_without_evidence_decodes` は旧 Criteria payload の criterion から `evidence` キーを省略し、`#[serde(default)]` により `None` として 1 passed。専用テスト追加前から契約が成立していたため RED にはならず、旧形状互換の明示的な回帰ロックとして保持する。
- contract: /home/turtton/.ghr/github.com/turtton/evorch/.opencode/v07-durable-execution-substrate-contract.md (host repo side)
- review notepad: .opencode/v07-durable-execution-substrate-review-notepad.md
- intent-cli binary: /nix/store/bhn4d645q3f0nc7m7k0iw20phizrsw5i-intent-cli-0.26.0/bin/intent-cli
- lead pane: w1X:p1

## Learnings

## Plan locked (plan agent ses_f4eda67ccffe19EaaiCY26FIaI)
13 tasks / 5 waves. Critical path T2→T4→T5→T6→T13.
W0: T1(storage v8), T2(event-bus variants+codes) — T9 deferred to W1 (needs T2 codes).
W1: T3(criterion wire), T4(ledger replay+durable fields), T7(budgets/checkpoints), T9(provider pre-spawn gate).
W2: T5(resume/retry/cancel), T8(escalation→NoProgress bridge), T10(typed reviewer result).
W3: T6(stale-worker), T11(supervisor typed-first), T12(GUI DurableTasks panel — new pane per v09-b pattern).
W4: T13(workspace green).
Decisions locked by plan: new GUI pane (not Agents extension); tasks-table column-add in v8 with widened CHECK; typed-first with prose fallback for reviewer; restore one-shot handled via fresh run per retry attempt.
