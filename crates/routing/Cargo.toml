[package]
name = "routing"
version.workspace = true
edition.workspace = true
publish = false

[lib]
name = "routing"

[dependencies]
config = { path = "../config" }
model = { path = "../model" }
providers = { path = "../providers" }
thiserror = { workspace = true }
