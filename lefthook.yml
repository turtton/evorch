pre-commit:
  jobs:
    - name: cargo fmt
      run: cargo fmt --all
      stage_fixed: true

pre-push:
  jobs:
    - name: Workspace clippy
      run: cargo clippy --workspace --all-targets -- -D warnings
    - name: Cache preservation contracts
      run: scripts/check-cache-contracts.sh
