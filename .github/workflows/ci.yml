name: ci

on:
  push:
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: 1.97.0
          components: rustfmt,clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo test --workspace

  # v0.1: offscreen 描画検証。wgpu の software Vulkan (lavapipe) で実 render し、
  # 生成 PNG を artifact として保存する（ADR 0009 / issue #9 の受理基準）。
  headless-capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: 1.97.0
      - uses: Swatinem/rust-cache@v2
      - name: Install headless rendering deps (Vulkan lavapipe / GL)
        run: sudo apt-get update && sudo apt-get install -y libvulkan1 mesa-vulkan-drivers libegl1 libgl1-mesa-dri libglx-mesa0
      - name: Run offscreen capture tests (real render, was #[ignore])
        run: cargo test -p gui --test offscreen -- --ignored --nocapture
        env:
          WGPU_BACKEND: vulkan
      - name: Capture frame via headless_capture bin
        run: cargo run -p gui --bin headless_capture -- /tmp/headless-capture.png
        env:
          WGPU_BACKEND: vulkan
      - uses: actions/upload-artifact@v4
        with:
          name: headless-capture
          path: /tmp/headless-capture.png
